import React from 'react'
import { S } from '../styles/globals'

export default function VistaPrestamos({ db }) {
  return (
    <div>
      <div style={S.pageHeader}>
        <div style={S.pageTitle}>Préstamos</div>
        <div style={S.pageSub}>Cartera completa de operaciones</div>
      </div>


      {/* Tu implementación aquí */}

      
    </div>
  )
}
